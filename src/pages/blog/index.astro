---
import { getCollection } from 'astro:content';
const posts = await getCollection('blog');
---

<html lang="en">
  <head>
    <title>AgentNet Blog</title>
  </head>
  <body>
    <h1>🧠 AgentNet Blog</h1>
    <p>A growing knowledge base for autonomous agents.</p>
    <ul>
      {posts.map(post => (
        <li>
          <a href={`/blog/${post.slug}`}>{post.data.title}</a>
          <span> — {post.data.pubDate}</span>
        </li>
      ))}
    </ul>
  </body>
</html>
